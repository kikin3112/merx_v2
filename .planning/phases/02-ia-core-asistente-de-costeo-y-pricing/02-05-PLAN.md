---
phase: 02-ia-core-asistente-de-costeo-y-pricing
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified: []
autonomous: false
requirements:
  - R1.1
must_haves:
  truths:
    - "OPENROUTER_API_KEY is set in Railway production environment variables"
    - "POST /recetas/{id}/asistente-ia returns 200 in production (not 503 or 500)"
    - "Full test suite backend/tests/ passes green with zero failures"
    - "Socia response in production uses correct Cali/Valle del Cauca tone and accessible language"
    - "Chat history is destroyed when the modal is closed and reopened"
  artifacts:
    - path: "backend/tests/test_servicio_ia_costeo.py"
      provides: "4 tests GREEN"
      contains: "test_analisis_inicial_returns_schema"
    - path: "backend/tests/test_recetas_endpoint.py"
      provides: "4 tests collected, test_asistente_ia_invalid_body GREEN"
      contains: "test_asistente_ia_invalid_body"
  key_links:
    - from: "Railway environment"
      to: "backend/app/config.py Settings.OPENROUTER_API_KEY"
      via: "Railway env var OPENROUTER_API_KEY set in dashboard"
      pattern: "OPENROUTER_API_KEY"
    - from: "frontend AsistenteCosteoPanel.tsx"
      to: "POST /recetas/{id}/asistente-ia"
      via: "recetas.consultarSocia() useMutation"
      pattern: "consultarSocia"
---

<objective>
Validate the complete Phase 2 feature end-to-end: confirm OPENROUTER_API_KEY is set in Railway production, run the full backend test suite, and perform a smoke test of the Socia assistant in production.

Purpose: Phase 2 ships to production only when the LLM key is confirmed set and the E2E flow works. This plan gates the phase completion — no code changes, only validation and a manual production smoke test.
Output: Confirmed working Socia assistant in production, full test suite green, phase marked complete in STATE.md.
</objective>

<execution_context>
@C:/Users/luisr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luisr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ia-core-asistente-de-costeo-y-pricing/02-CONTEXT.md
@.planning/phases/02-ia-core-asistente-de-costeo-y-pricing/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite and verify all tests pass</name>
  <files></files>
  <action>
    Run the complete backend test suite and verify zero failures.

    Execute sequentially:

    1. Run focused Socia service tests:
       ```bash
       cd /c/Users/luisr/PycharmProjects/merx_v2 && uv run pytest backend/tests/test_servicio_ia_costeo.py -v
       ```
       Expected: 4 passed, 0 failed.

    2. Run endpoint integration tests:
       ```bash
       cd /c/Users/luisr/PycharmProjects/merx_v2 && uv run pytest backend/tests/test_recetas_endpoint.py -v
       ```
       Expected: At minimum `test_asistente_ia_invalid_body` passes (422 is pure Pydantic validation). Others may show auth-gated results (401/403) which is acceptable.

    3. Run full test suite:
       ```bash
       cd /c/Users/luisr/PycharmProjects/merx_v2 && uv run pytest backend/tests/ -q --tb=short
       ```
       Expected: All existing tests remain green. No regressions introduced by Phase 2 changes.

    If any test fails:
    - Check if it is a pre-existing failure (compare with git blame on test file)
    - If pre-existing: note in SUMMARY.md and proceed
    - If introduced by Phase 2 changes: fix before proceeding to Task 2
  </action>
  <verify>
    <automated>cd /c/Users/luisr/PycharmProjects/merx_v2 && uv run pytest backend/tests/test_servicio_ia_costeo.py backend/tests/test_recetas_endpoint.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>test_servicio_ia_costeo.py: 4 passed; test_recetas_endpoint.py: test_asistente_ia_invalid_body passes; full suite shows no new regressions</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set OPENROUTER_API_KEY in Railway production</name>
  <files></files>
  <action>Verify or set OPENROUTER_API_KEY in Railway production environment variables.</action>
  <verify>Manual — confirm key is present in Railway dashboard (OpenRouter keys start with sk-or-).</verify>
  <done>OPENROUTER_API_KEY confirmed set in Railway. Backend will no longer return 503 due to missing key.</done>
  <what-built>
    Phase 2 backend is complete and tested locally. Before production smoke test, the Railway OPENROUTER_API_KEY environment variable must be verified (or set if missing). The backend uses OpenRouter (not Anthropic) via httpx to openrouter.ai/api/v1/chat/completions.
  </what-built>
  <how-to-verify>
    This is a Railway dashboard action — Claude cannot do this via CLI.

    1. Go to https://railway.app and open the Merx v2 project
    2. Select the backend service
    3. Go to Variables tab
    4. Search for OPENROUTER_API_KEY
    5. If it EXISTS: confirm the value looks like an OpenRouter key — action complete, type "confirmed"
    6. If it DOES NOT EXIST:
       a. Click "Add Variable"
       b. Name: OPENROUTER_API_KEY
       c. Value: (your OpenRouter API key from openrouter.ai/keys)
       d. Save — Railway will redeploy automatically
       e. Wait for deployment to complete (2-3 minutes)
       f. Type "set-and-deployed"

    Note: Also check OPENROUTER_MODEL is optionally set (default: arcee-ai/trinity-large-preview:free).
    If calls fail with 429 or 403, check openrouter.ai credits.
  </how-to-verify>
  <resume-signal>Type "confirmed" if OPENROUTER_API_KEY already set, or "set-and-deployed" after adding it</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Production smoke test — Socia end-to-end in Railway</name>
  <files></files>
  <action>Smoke test the complete Phase 2 feature in Railway production environment.</action>
  <verify>Manual — follow the production smoke test checklist below.</verify>
  <done>Socia responds correctly in production: Fase 1 analysis renders, Fase 2 chat works, state resets on close.</done>
  <what-built>
    Complete Phase 2 feature deployed to production:
    - Socia LLM service (ServicioIACosteo) with tool_use + prompt caching
    - POST /recetas/{id}/asistente-ia endpoint with tenant isolation
    - AsistenteCosteoPanel modal with Fase 1 analysis + Fase 2 chat
    - "Consultar a Socia" button in the Analisis tab of RecetasPage
  </what-built>
  <how-to-verify>
    **Production smoke test** — use the Luz De Luna tenant in production:

    1. Go to production frontend URL (Railway frontend deployment)
    2. Log in as luzdelunavelas30@gmail.com (password: admin123)
    3. Select tenant "Luz De Luna" if prompted
    4. Navigate to Recetas -> tab "Analisis de precios"
    5. Select "VELA VASO" (or any recipe that has been calculated previously)
    6. Verify: "Consultar a Socia" amber gradient button appears below EscenariosPrecios
    7. Click the button — verify: modal opens with loading spinner
    8. Wait up to 10 seconds for Fase 1 response

    **Fase 1 checks:**
    - precio_sugerido renders as currency (e.g. "$25,000")
    - margen_esperado renders as percentage (e.g. "48.5%")
    - escenario_recomendado shows one of the 5 known scenario names
    - justificacion is in Spanish, accessible language (NOT accounting jargon)
    - alertas list shows at least 1 alert (or empty list — both valid)
    - mensaje_cierre is warm and closing (Socia personality)
    - "Tienes mas preguntas para Socia?" prompt appears at the bottom

    **Fase 2 checks:**
    - Click "Si, preguntarle"
    - Type: "Cuanto deberia cobrar si quiero ganar mas?"
    - Socia responds in conversational Spanish — warm, accessible tone
    - No HTTP errors appear in the UI

    **State cleanup check:**
    - Close modal with X
    - Reopen modal by clicking "Consultar a Socia" again
    - Verify: fresh analysis loads (not the previous conversation)

    **Failure scenarios to note (not blockers):**
    - Slow response (>10 seconds): Acceptable — Haiku can be slow under load
    - Generic error message "Socia no pudo responder": Check Railway logs for ANTHROPIC_API_KEY or credits issue
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific failures for gap closure</resume-signal>
</task>

</tasks>

<verification>
Phase 2 complete when:
- `uv run pytest backend/tests/test_servicio_ia_costeo.py -v` — 4 passed
- `uv run pytest backend/tests/ -q` — full suite green (no new failures)
- OPENROUTER_API_KEY confirmed present in Railway environment variables
- Production smoke test: Fase 1 response renders with all 6 fields
- Production smoke test: Fase 2 chat sends and receives response
- Production smoke test: Modal state resets on close+reopen
</verification>

<success_criteria>
- All Phase 2 tests pass in CI/CD-equivalent run (local uv run pytest)
- ANTHROPIC_API_KEY set in Railway production before go-live
- Socia responds correctly in production environment for Luz De Luna tenant
- No 503 errors from missing OPENROUTER_API_KEY or httpx failures in Railway logs
- Phase 2 marked COMPLETE in STATE.md and ROADMAP.md
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/02-ia-core-asistente-de-costeo-y-pricing/02-05-SUMMARY.md`
2. Update `.planning/STATE.md` — mark Phase 2 as COMPLETE, note current position at Phase 3
3. Update `.planning/ROADMAP.md` — Phase 2 status: COMPLETO
</output>
