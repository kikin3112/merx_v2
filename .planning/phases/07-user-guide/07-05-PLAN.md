---
phase: 07-user-guide
plan: '05'
type: execute
wave: 5
depends_on: ['07-01', '07-02', '07-03', '07-04']
files_modified:
  - backend/app/servicios/guia_usuario/chapters/storage.py
  - backend/app/servicios/guia_usuario/chapters/payments.py
  - backend/app/servicios/guia_usuario/__main__.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Storage chapter documents S3/R2 file storage for PDFs and logos"
    - "Payments chapter covers Wompi subscription flow"
    - "Complete PDF generates with all 13+ chapters and table of contents"
  artifacts:
    - path: "backend/app/servicios/guia_usuario/chapters/storage.py"
      provides: "Storage chapter with S3/R2 explanation"
      min_lines: 50
    - path: "backend/app/servicios/guia_usuario/chapters/payments.py"
      provides: "Payments chapter with Wompi subscription flow"
      min_lines: 50
    - path: "backend/app/servicios/guia_usuario/__main__.py"
      provides: "CLI entry point to generate PDF"
      min_lines: 30
  key_links:
    - from: "__main__.py"
      to: "generator.py"
      via: "GeneratorGuiaUsuario class"
      pattern: "GeneratorGuiaUsuario"
    - from: "__main__.py"
      to: "chapters/*"
      via: "Import all chapter classes"
      pattern: "from.*chapters import"
---

<objective>
Create chapters for Storage and Payments, then assemble final PDF with all chapters.

Purpose: Complete the user guide with integration chapters and generate the final PDF document.

Output: Complete PDF user guide with all functionalities documented.
</objective>

<execution_context>
@C:/Users/luisr/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/luisr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-guide/07-CONTEXT.md
@.planning/phases/07-user-guide/07-01-PLAN.md
@.planning/phases/07-user-guide/07-02-SUMMARY.md
@.planning/phases/07-user-guide/07-03-SUMMARY.md
@.planning/phases/07-user-guide/07-04-SUMMARY.md

# All content in Spanish per user decision
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Storage chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/storage.py
  </files>
  <action>
    Create storage.py with StorageChapter class:
    - Chapter title: "Almacenamiento (Storage)"
    - Section: "¿Qué se almacena?" - Storage objects:
      - Logos de tenant (configuración)
      - PDFs de facturas
      - PDFs de cotizaciones
    - Section: "S3/R2" - Storage backend:
      - Explica que usa S3 compatible (AWS, Cloudflare R2, DigitalOcean)
      - Buckets: chandelier-logos (público), chandelier-facturas (privado)
      - Naming: {tenant_id}/{tipo}/{uuid}.{ext}
    - Section: "URLs de Acceso" - Access patterns:
      - Logos: URL pública directa
      - Facturas: Presigned URLs con expiración (24h)
    - Section: "Límites" - Limits:
      - Logos: max 2MB
      - PDFs: max 5MB
    - Feature table: Almacenamiento S3, Presigned URLs, Limites por tipo
    - Screenshot placeholders: Config upload, Storage settings
  </action>
  <verify>
    Import and verify StorageChapter:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.storage import StorageChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = StorageChapter(get_styles())
print('Storage elements:', len(ch.build()))
"`
  </verify>
  <done>
    Storage chapter complete with S3/R2 explanation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Payments (Wompi) chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/payments.py
  </files>
  <action>
    Create payments.py with PaymentsChapter class:
    - Chapter title: "Pagos Online (Wompi)"
    - Section: "Suscripciones SaaS" - Wompi integration:
      - Tenants pagan mensualidad por usar el sistema
      - Planes: Básico, Profesional, Empresarial (detalles en otra sección)
    - Section: "Flujo de Pago" - Payment flow:
      1. Usuario selecciona plan
      2. Redirect a checkout Wompi
      3. Usuario ingresa datos tarjeta
      4. Wompi procesa pago
      5. Webhook notifica resultado
      6. Sistema actualiza suscripción
    - Section: "Estados de Suscripción" - Subscription states:
      - Activa, Suspendida, Cancelada, Vencida
    - Section: "Webhook Wompi" - Automatic handling:
      - Sistema recibe notificación de pago
      - Actualiza estado suscripción
      - Envía email confirmación
    - Section: "Renovación" - Auto-renewal:
      - Verifica fecha_proximo_pago diario
      - Suspende si no hay pago después de 3 intentos
    - Feature table: Pago con Wompi, Webhook automático, Renovación automática, Notificaciones
    - Screenshot placeholders: Plan selection, Payment form, Subscription status
  </action>
  <verify>
    Import and verify PaymentsChapter:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.payments import PaymentsChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = PaymentsChapter(get_styles())
print('Payments elements:', len(ch.build()))
"`
  </verify>
  <done>
    Payments chapter complete with Wompi subscription flow
  </done>
</task>

<task type="auto">
  <name>Task 3: Create PDF generation CLI and assemble complete guide</name>
  <files>
    - backend/app/servicios/guia_usuario/__main__.py
  </files>
  <action>
    Create __main__.py with CLI entry point:
    1. Import all chapter classes:
       - QuickStartChapter, AuthChapter, ProductsChapter
       - MarginCalculatorChapter, CRMChapter
       - QuotationsChapter, BillingChapter, POSChapter
       - RecipesChapter, AccountingChapter, DashboardChapter
       - StorageChapter, PaymentsChapter
    2. Create main() function:
       - Initialize GeneratorGuiaUsuario
       - Add title page: "Guía de Usuario Chandelier ERP/POS"
       - Add subtitle: "Sistema ERP/POS multi-tenant para microempresas"
       - Add version and date
       - Add TOC
       - Add all chapters in order
       - Add page breaks between major sections
       - Build PDF
    3. Add CLI arguments:
       - --output (-o): Output path (default: guia_usuario.pdf)
       - --version (-v): Show version
    4. Add __main__ guard for direct execution
    
    Run: `python -m backend.app.servicios.guia_usuario -o guia_usuario.pdf`
  </action>
  <verify>
    Test PDF generation:
    `cd backend && python -m app.servicios.guia_usuario -o test_guia.pdf && ls -la test_guia.pdf`
    Verify PDF has 10+ pages (all chapters).
  </verify>
  <done>
    CLI generates complete PDF with all 13+ chapter modules
  </done>
</task>

</tasks>

<verification>
- [ ] All chapter imports work
- [ ] Storage chapter documents S3/R2 for PDFs/logos
- [ ] Payments chapter covers Wompi subscription flow
- [ ] Complete PDF generates with all chapters
- [ ] PDF has proper table of contents
- [ ] CLI entry point works correctly
</verification>

<success_criteria>
Complete PDF user guide generated with all functionalities documented in Spanish.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-guide/07-05-SUMMARY.md`
</output>
