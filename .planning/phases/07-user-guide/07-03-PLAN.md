---
phase: 07-user-guide
plan: '03'
type: execute
wave: 3
depends_on: ['07-01']
files_modified:
  - backend/app/servicios/guia_usuario/chapters/quotations.py
  - backend/app/servicios/guia_usuario/chapters/billing.py
  - backend/app/servicios/guia_usuario/chapters/pos.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Quotations chapter explains full lifecycle from creation to conversion"
    - "Billing chapter documents PDF generation and numbering per tenant"
    - "POS chapter covers mobile-first interface with product grid and cart"
  artifacts:
    - path: "backend/app/servicios/guia_usuario/chapters/quotations.py"
      provides: "Quotations chapter with create, edit, convert workflow"
      min_lines: 60
    - path: "backend/app/servicios/guia_usuario/chapters/billing.py"
      provides: "Billing chapter with PDF generation and numbering"
      min_lines: 60
    - path: "backend/app/servicios/guia_usuario/chapters/pos.py"
      provides: "POS chapter with mobile-first POS workflow"
      min_lines: 60
  key_links:
    - from: "chapters/quotations.py"
      to: "chapters/crm.py"
      via: "Client selection in quotations"
      pattern: "seleccionar cliente"
    - from: "chapters/billing.py"
      to: "servicio_pdf.py"
      via: "PDF generation patterns"
      pattern: "generar.*pdf"
---

<objective>
Create chapters for Quotations, Billing, and POS - covering the complete sales workflow.

Purpose: Document sales and point-of-sale functionalities per user requirement for "Functionalities" section.

Output: Three complete chapter modules for the PDF user guide.
</objective>

<execution_context>
@C:/Users/luisr/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/luisr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-guide/07-CONTEXT.md
@.planning/phases/07-user-guide/07-01-PLAN.md

# Reference existing PDF service for billing patterns
@backend/app/servicios/servicio_pdf.py

# All content in Spanish per user decision
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quotations chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/quotations.py
  </files>
  <action>
    Create quotations.py with QuotationsChapter class:
    - Chapter title: "Cotizaciones"
    - Section: "Crear Cotización" - Step by step:
      1. Seleccionar cliente (o crear nuevo)
      2. Agregar productos con cantidad y precio
      3. Aplicar descuentos (por línea o global)
      4. Definir fecha de validez
      5. Guardar como borrador o enviar
    - Section: "Estados de Cotización" - State diagram/flow:
      - Borrador → Enviada → Aceptada/Rechazada → Convertida
    - Section: "Convertir a Factura" - One-click conversion workflow
    - Section: "Validez de Precios" - Explain fecha_vencimiento
    - Feature table: Crear, Editar, Enviar, Convertir a factura
    - Screenshot placeholders: Quotations list, Create form, Conversion dialog
    Reference CRM chapter for client selection.
  </action>
  <verify>
    Import and verify QuotationsChapter:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.quotations import QuotationsChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = QuotationsChapter(get_styles())
print('Quotations elements:', len(ch.build()))
"`
  </verify>
  <done>
    Quotations chapter covers create, states, and conversion workflow
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Billing (Invoicing) chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/billing.py
  </files>
  <action>
    Create billing.py with BillingChapter class:
    - Chapter title: "Facturación"
    - Section: "Crear Factura" - Step by step:
      1. Seleccionar cliente (requerido)
      2. Agregar productos (valida stock disponible)
      3. Sistema calcula subtotal, IVA, total automáticamente
      4. Guardar como borrador
    - Section: "Emitir Factura" - Final step:
      1. Confirmar emisión
      2. Sistema genera PDF automáticamente
      3. PDF se guarda en Storage (S3/R2)
      4. Se crea movimiento de stock (salida)
      5. Se genera asiento contable automático
    - Section: "Numeración" - Explain tenant-specific numbering:
      - Formato: {prefijo}-{número} (ej: FAC-001)
      - Configurable en Settings
    - Section: "Estados de Factura" - Borrador, Emitida, Pagada, Anulada
    - Section: "Anular Factura" - Reversa stock y genera asiento de reversión
    - Feature table: PDF generation, Numeración automática, Asiento contable
    - Screenshot placeholders: Invoice form, PDF output, Invoice list with states
    Reference existing servicio_pdf.py patterns for PDF generation details.
  </action>
  <verify>
    Import and verify BillingChapter:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.billing import BillingChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = BillingChapter(get_styles())
print('Billing elements:', len(ch.build()))
"`
  </verify>
  <done>
    Billing chapter covers create, emit, PDF generation, numbering, and states
  </done>
</task>

<task type="auto">
  <name>Task 3: Create POS chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/pos.py
  </files>
  <action>
    Create pos.py with POSChapter class:
    - Chapter title: "Punto de Venta (POS)"
    - Section: "Interfaz POS" - Describe layout:
      - Grid de productos (izquierda)
      - Carrito de compras (derecha)
      - Total calculado en tiempo real
    - Section: "Flujo de Venta" - Step by step:
      1. Buscar o seleccionar productos del grid
      2. Click en producto para agregar al carrito
      3. Ajustar cantidades (+/-)
      4. Seleccionar cliente (opcional, usa "Cliente Mostrador" por defecto)
      5. Click "Cobrar" → Genera factura automáticamente
    - Section: "Cliente Mostrador" - Default client:
      - NIT: 222222222222
      - Usado cuando no se selecciona cliente específico
    - Section: "Consideraciones Móvil" - Mobile-first features:
      - Diseño responsivo
      - Touch-friendly buttons (48x48px mínimo)
      - Bottom tab bar navigation
    - Feature table: Grid productos, Carrito, Cobro rápido, Factura automática
    - Screenshot placeholders: POS desktop, POS mobile, Cart view
    Document per user requirement for "POS mobile-first".
  </action>
  <verify>
    Import and verify POSChapter:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.pos import POSChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = POSChapter(get_styles())
print('POS elements:', len(ch.build()))
"`
  </verify>
  <done>
    POS chapter covers product grid, cart, checkout, and mobile-first design
  </done>
</task>

</tasks>

<verification>
- [ ] All chapter imports work
- [ ] State diagrams/flows are clearly documented
    - [ ] Quotations: Borrador → Enviada → Aceptada/Rechazada/Convertida
    - [ ] Billing: Borrador → Emitida → Pagada/Anulada
    - [ ] POS: Product selection → Cart → Checkout
- [ ] Screenshot placeholders added appropriately
- [ ] Feature tables included
- [ ] Step-by-step instructions numbered
</verification>

<success_criteria>
Three chapter modules (Quotations, Billing, POS) complete and ready for PDF generation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-guide/07-03-SUMMARY.md`
</output>
