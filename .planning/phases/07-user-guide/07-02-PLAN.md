---
phase: 07-user-guide
plan: '02'
type: execute
wave: 2
depends_on: ['07-01']
files_modified:
  - backend/app/servicios/guia_usuario/chapters/quickstart.py
  - backend/app/servicios/guia_usuario/chapters/auth.py
  - backend/app/servicios/guia_usuario/chapters/products.py
  - backend/app/servicios/guia_usuario/chapters/margin_calc.py
  - backend/app/servicios/guia_usuario/chapters/crm.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Quick Start chapter helps new users get started in under 5 minutes"
    - "Each functionality chapter includes purpose, features, and screenshot placeholders"
    - "Step-by-step instructions are clear and numbered for easy following"
  artifacts:
    - path: "backend/app/servicios/guia_usuario/chapters/quickstart.py"
      provides: "Quick Start chapter with first login, tenant selection, dashboard tour"
      min_lines: 60
    - path: "backend/app/servicios/guia_usuario/chapters/auth.py"
      provides: "Authentication & Multi-tenancy chapter"
      min_lines: 50
    - path: "backend/app/servicios/guia_usuario/chapters/products.py"
      provides: "Products & Inventory chapter with CRUD and stock management"
      min_lines: 60
  key_links:
    - from: "chapters/quickstart.py"
      to: "generator.py"
      via: "add_chapter() method"
      pattern: "add_chapter.*QuickStartChapter"
    - from: "chapters/products.py"
      to: "templates/chapter.py"
      via: "ChapterBuilder class"
      pattern: "ChapterBuilder"
---

<objective>
Create chapters for Quick Start, Authentication, Products & Inventory, Margin Calculator, and CRM Clients.

Purpose: Document core business functionalities that new users need first, following the user requirement structure (Quick Start → Functionalities → Manual).

Output: Five complete chapter modules ready to add to the PDF generator.
</objective>

<execution_context>
@C:/Users/luisr/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/luisr/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-user-guide/07-CONTEXT.md
@.planning/phases/07-user-guide/07-01-PLAN.md
@.planning/phases/07-user-guide/07-01-SUMMARY.md

# Use ChapterBuilder from Plan 07-01
# All chapters in Spanish per user decision
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quick Start chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/__init__.py
    - backend/app/servicios/guia_usuario/chapters/quickstart.py
  </files>
  <action>
    Create chapters module and QuickStartChapter:
    1. `chapters/__init__.py` - Export all chapter classes
    2. `chapters/quickstart.py` - QuickStartChapter class with build() method:
       - Chapter 1: Introducción (1 página)
         - ¿Qué es Chandelier ERP/POS?
         - Requisitos del sistema
       - Chapter 2: Primer Inicio de Sesión (2 páginas)
         - Paso 1: Acceder al sistema
         - Paso 2: Ingresar credenciales
         - Paso 3: Seleccionar tenant
         - Screenshot: Pantalla de login
       - Chapter 3: Tour del Dashboard
         - Descripción de KPIs principales
         - Navegación básica
         - Screenshot: Dashboard con anotaciones
       Use ChapterBuilder from templates, add_step_list for numbered steps.
       Include "Tiempo estimado: 5 minutos" intro text.
  </action>
  <verify>
    Import and instantiate QuickStartChapter, verify it returns list:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.quickstart import QuickStartChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = QuickStartChapter(get_styles())
print('Elements:', len(ch.build()))
"`
  </verify>
  <done>
    Quick Start chapter complete with first login steps and dashboard tour
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Authentication & Multi-tenancy chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/auth.py
  </files>
  <action>
    Create auth.py with AuthChapter class:
    - Chapter title: "Autenticación y Multi-tenancy"
    - Section: "¿Qué es JWT?" - Explanation of secure authentication
    - Section: "Roles de Usuario" - Admin, Vendedor, Contador with permissions table
    - Section: "Cambiar de Tenant" - How to switch between companies
    - Section: "Cerrar Sesión" - Security best practices
    - Feature table: {Funcionalidad, Descripción, Disponible}
      - JWT con expiración 1h | Sí
      - Refresh tokens 7 días | Sí
      - RLS PostgreSQL | Sí
      - Roles por tenant | Sí
    - Screenshot placeholders: Login screen, Role selection dropdown
    Use Spanish terminology per user requirement.
  </action>
  <verify>
    Import and verify AuthChapter builds correctly:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.auth import AuthChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = AuthChapter(get_styles())
print('Auth chapter elements:', len(ch.build()))
"`
  </verify>
  <done>
    Authentication chapter covers JWT, roles, multi-tenant switching
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Products & Inventory chapter</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/products.py
  </files>
  <action>
    Create products.py with ProductsChapter class:
    - Chapter title: "Productos e Inventario"
    - Section: "Crear Producto" - Form fields: SKU, nombre, tipo, categoría, precio, costo, stock, IVA
    - Section: "Tipos de Producto" - Producto terminado vs Materia prima
    - Section: "Valorización de Inventario" - Weighted average costing formula with example
      - Formula: CPP = (Stock × Costo + Entrada × Costo_nuevo) / (Stock + Entrada)
    - Section: "Alertas de Stock" - Configurar mínimo y notificaciones
    - Section: "Movimientos de Stock" - Tipos: entrada, salida, ajuste, producción
    - Steps for creating product: 5 numbered steps
    - Screenshot placeholders: Product list, Product form, Stock alert config
    Include feature table with CRUD, stock alerts, weighted average.
  </action>
  <verify>
    Import and verify ProductsChapter:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.products import ProductsChapter
from backend.app.servicios.guia_usuario.styles import get_styles
ch = ProductsChapter(get_styles())
print('Products chapter elements:', len(ch.build()))
"`
  </verify>
  <done>
    Products chapter covers CRUD, inventory valuation, stock alerts, movements
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Margin Calculator and CRM chapters</name>
  <files>
    - backend/app/servicios/guia_usuario/chapters/margin_calc.py
    - backend/app/servicios/guia_usuario/chapters/crm.py
  </files>
  <action>
    Create two chapter files:
    
    1. `margin_calc.py` - MarginCalculatorChapter:
       - Chapter title: "Calculadora de Márgenes"
       - Section: "¿Cómo funciona?" - Formula explanation
         - Precio = Costo / (1 - Margen/100)
       - Example: Costo $1,614, Margen 60% → Precio $4,035
       - Section: "Usar la calculadora" - Step by step in product form
       - Include visual formula reference
    
    2. `crm.py` - CRMChapter:
       - Chapter title: "CRM - Gestión de Clientes"
       - Section: "Crear Cliente" - Fields: NIT, nombre, email, teléfono, dirección
       - Section: "Retención IVA" - Explain retención_iva toggle
       - Section: "Cliente Mostrador" - Default client for POS
       - Feature table: Gestión de clientes, Historial de compras, Retención IVA
       - Screenshot placeholders: Client list, Client form
    Use Spanish per user requirement.
  </action>
  <verify>
    Import both chapters:
    `python -c "
from backend.app.servicios.guia_usuario.chapters.margin_calc import MarginCalculatorChapter
from backend.app.servicios.guia_usuario.chapters.crm import CRMChapter
from backend.app.servicios.guia_usuario.styles import get_styles
s = get_styles()
print('Margin:', len(MarginCalculatorChapter(s).build()))
print('CRM:', len(CRMChapter(s).build()))
"`
  </verify>
  <done>
    Margin Calculator and CRM chapters complete
  </done>
</task>

</tasks>

<verification>
- [ ] All chapter imports work
- [ ] Chapters use ChapterBuilder correctly
- [ ] Spanish text is properly formatted
- [ ] Screenshot placeholders added where appropriate
- [ ] Feature tables included for key functionalities
</verification>

<success_criteria>
Five chapter modules (Quick Start, Auth, Products, Margin Calculator, CRM) ready for PDF generation.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-guide/07-02-SUMMARY.md`
</output>
