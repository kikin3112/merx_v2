[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'alembic upgrade head && uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT --workers 2'"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
