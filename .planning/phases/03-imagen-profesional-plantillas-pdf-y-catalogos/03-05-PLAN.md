---
phase: 03-imagen-profesional-plantillas-pdf-y-catalogos
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
autonomous: false
files_modified:
  - backend/tests/test_almacenamiento.py
  - backend/tests/test_upload_logo.py
  - backend/tests/test_servicio_pdf.py
  - backend/tests/test_catalogo_pdf.py
  - backend/tests/test_upload_producto_imagen.py
  - .planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-SUMMARY.md
requirements:
  - R4.1
  - R4.2

must_haves:
  truths:
    - "Suite completa pytest -x -q sale verde (0 failures)"
    - "PDF de factura en producción muestra logo y color_primario del tenant (verificación humana)"
    - "Catálogo PDF en producción descarga correctamente desde CatalogoPage (verificación humana)"
    - "SUMMARY.md documenta decisiones, artefactos creados y patrones establecidos"
  artifacts:
    - path: "backend/tests/test_almacenamiento.py"
      provides: "Full test suite for S3 image methods"
    - path: "backend/tests/test_servicio_pdf.py"
      provides: "Full test suite for branded PDF + WCAG"
    - path: ".planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-SUMMARY.md"
      provides: "Phase completion summary"
      contains: "R4.1"
  key_links:
    - from: "pytest full suite"
      to: "all Phase 3 test files"
      via: "cd backend && pytest -x -q"
    - from: "production smoke test"
      to: "Railway deployed backend"
      via: "curl POST /tenants/me/logo + GET /facturas/{id}/pdf"
---

<objective>
Full-suite validation of Phase 3. Ensure all 20+ tests are green, run a production smoke test confirming branding works on Railway, and produce the phase SUMMARY.md.

Purpose: Gate before Phase 4. Verifies that R4.1 (PDF branding) and R4.2 (catalogue) are working end-to-end in production, not just locally.

Output: Green pytest suite, human-confirmed production PDF with branding, SUMMARY.md.
</objective>

<execution_context>
@C:/Users/luisr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/luisr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-VALIDATION.md
@.planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-01-SUMMARY.md
@.planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-02-SUMMARY.md
@.planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-03-SUMMARY.md
@.planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite validation + gap fixes</name>
  <files>
    backend/tests/test_almacenamiento.py,
    backend/tests/test_upload_logo.py,
    backend/tests/test_servicio_pdf.py,
    backend/tests/test_catalogo_pdf.py,
    backend/tests/test_upload_producto_imagen.py
  </files>
  <action>
Run the full backend test suite. Fix any failures found. Minimum expected count: 20 new tests across the 5 Phase 3 test files (on top of the existing 45+ from prior phases).

**Step 1 — Run full suite:**
```bash
cd backend && python -m pytest -x -q 2>&1
```

**Step 2 — For each failure:**
- Read the error message
- Identify the file and line
- Apply targeted fix (do NOT rewrite passing tests)
- Re-run the specific test to confirm green before moving on

**Common failure patterns to watch for:**

1. Import errors in new test files — check that `conftest.py` fixtures are imported correctly (look at test_recetas_endpoint.py for the import pattern)

2. `imagen_s3_key` attribute not on `Producto` model at test time — ensure the Alembic migration was applied before running tests: `alembic upgrade head`

3. `_wcag_text_color` not importable — it's a module-level function; import as: `from app.servicios.servicio_pdf import _wcag_text_color`

4. `generar_catalogo_pdf` requires `self.primary_color` set — ensure tests create `ServicioPDF` with a `tenant_info` dict that includes `color_primario`

5. StreamingResponse content-type in test — FastAPI `TestClient` returns `response.headers["content-type"]`; check it starts with `"application/pdf"` (may include charset)

6. Route ordering conflict in productos.py — `POST /catalogo-pdf` must be registered BEFORE `GET /{producto_id}`. If tests get 422 "unprocessable" on /catalogo-pdf, verify route order in productos.py.

**Step 3 — Confirm count:**
```bash
cd backend && python -m pytest tests/test_almacenamiento.py tests/test_upload_logo.py tests/test_servicio_pdf.py tests/test_catalogo_pdf.py tests/test_upload_producto_imagen.py -v 2>&1 | tail -30
```
Should show 20+ tests, all green.

**Step 4 — Run full existing suite to confirm no regressions:**
```bash
cd backend && python -m pytest -x -q 2>&1
```
All prior tests (test_recetas_calculo.py, test_recetas_endpoint.py, test_rbac_backend.py, test_servicio_ia_costeo.py, test_inventario_concurrency.py) must remain green.
  </action>
  <verify>
    <automated>cd /c/Users/luisr/PycharmProjects/merx_v2/backend && python -m pytest -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full pytest suite green — 0 failures. Phase 3 test files collectively have 20+ tests. No regressions in prior test files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 implementation deployed to Railway production: S3 image infrastructure, branded PDFs (logo + tenant colors), ConfigPage Marca tab, CatalogoPage with PDF generation. Full local test suite is green.
  </what-built>
  <how-to-verify>
    **Pre-check — Deploy to production:**
    ```bash
    cd backend && railway up --detach
    cd frontend && npx vercel --prod
    ```
    Wait for deploy confirmation before testing.

    **Test 1 — Branding configuration (R4.1):**
    1. Log in to production as admin (luzdelunavelas30@gmail.com / admin123)
    2. Go to Configuración → Marca tab
    3. Upload a logo (any PNG under 2MB)
    4. Change color primario to a distinctive color (e.g. #E84393 — hot pink)
    5. Click "Guardar colores"
    6. Go to Facturas, emit a test invoice (or open an existing one)
    7. Download the PDF — verify the header shows the hot pink color
    8. Verify the logo appears in the PDF header

    **Test 2 — Catálogo PDF (R4.2):**
    1. Navigate to /catalogo in production
    2. Verify product grid loads with actual products
    3. Select 2-3 products using checkboxes
    4. Click "Generar PDF (N)"
    5. Verify PDF opens in new tab
    6. Verify PDF contains: company header with brand color, product names and prices

    **Test 3 — Regression check:**
    1. Navigate to Facturas — verify existing PDF download still works
    2. Navigate to Cotizaciones — verify quotes still load
    3. Navigate to Recetas → Socia button — verify IA assistant still responds

    Report: "approved" if all 3 tests pass, or describe the first failing step.
  </how-to-verify>
  <resume-signal>Type "approved" when production PDF shows tenant branding and catalogue PDF downloads successfully. Describe any failures found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write Phase 3 SUMMARY.md</name>
  <files>
    .planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-SUMMARY.md
  </files>
  <action>
Read the four plan SUMMARYs (03-01 through 03-04) and the SUMMARY template at @C:/Users/luisr/.claude/get-shit-done/templates/summary.md. Then write the consolidated phase SUMMARY.md.

The SUMMARY must include:

**Requirements delivered:**
- R4.1: PDF branding — logo from S3, color_primario/color_secundario applied to facturas and cotizaciones
- R4.2: Catalogue PDF — streaming generation, product image upload, CatalogoPage

**Architecture decisions made:**
- ReportLab only (no WeasyPrint/Puppeteer) — existing dependency, Railway compatible
- S3 key stored in url_logo (not presigned URL) — keys are permanent, URLs expire
- Catalogue always streaming (not S3-cached) — no storage cost for on-demand generation
- WCAG luminance threshold 0.5 for auto text contrast
- productos.imagen_s3_key nullable Text — no default, graceful degradation (no image = no image in PDF)

**Artifacts created (exact file paths):**
- backend/app/servicios/servicio_almacenamiento.py — added subir_imagen(), obtener_imagen_bytes()
- backend/app/rutas/tenants.py — added POST /me/logo, PATCH /me
- backend/alembic/versions/add_imagen_s3_key_to_productos.py — migration
- backend/app/servicios/servicio_pdf.py — _wcag_text_color(), branded generar_*_pdf(), generar_catalogo_pdf()
- backend/app/rutas/facturas.py — _get_tenant_info() now includes colors
- backend/app/rutas/productos.py — POST /catalogo-pdf, POST/{id}/imagen, DELETE/{id}/imagen
- frontend/src/pages/CatalogoPage.tsx — new page
- frontend/src/components/config/MarcaTab.tsx — new component
- frontend/src/pages/ConfigPage.tsx — tab shell
- frontend/src/hooks/useNavigation.ts — /catalogo in Comercial group
- frontend/src/App.tsx — /catalogo route
- frontend/src/api/endpoints.ts — getMe, updateMe, uploadLogoMe, generarCatalogoPdf, subirImagen, eliminarImagen
- frontend/src/types/index.ts — Producto.imagen_s3_key

**Test count:** 20+ new tests across 5 test files

**Patterns established:**
- S3 image path convention: `tenants/{tenant_id}/{sub_path}.{ext}`
- PDF branding injection: via tenant_info dict, not constructor overloading
- Catalogue streaming: StreamingResponse + ArrayBuffer on frontend, no S3 caching
- Carbon Vivo tab pill pattern: `cv-badge cv-badge-primary/cv-badge-neutral + cursor-pointer`

**Known limitations / future work:**
- Product images not displayed as actual images in CatalogoPage grid (shows "IMG" label) — Phase 5 can add presigned URL display
- No image crop/resize on upload — large images may affect PDF layout
- Catalogue PDF always regenerated on demand — acceptable for current scale
  </action>
  <verify>
    <automated>ls /c/Users/luisr/PycharmProjects/merx_v2/.planning/phases/03-imagen-profesional-plantillas-pdf-y-catalogos/03-SUMMARY.md 2>&1</automated>
  </verify>
  <done>03-SUMMARY.md exists with all required sections. Phase 3 marked complete.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest -x -q` — 0 failures, 60+ total tests
- Production: factura PDF shows tenant color_primario in header
- Production: CatalogoPage generates downloadable PDF
- 03-SUMMARY.md written with R4.1, R4.2 documented
</verification>

<success_criteria>
- pytest full suite green (0 failures, no regressions)
- Production smoke test confirms branding + catalogue both functional
- SUMMARY.md complete with patterns, decisions, and artifact list
- Phase 3 status can be updated to COMPLETO in ROADMAP.md
</success_criteria>

<output>
After completion, update `.planning/ROADMAP.md` Phase 3 status to COMPLETO and update the plans list:
```
Plans: 5/5 plans executed
- [x] 03-01-PLAN.md — S3 infraestructura + logo upload + migración Productos
- [x] 03-02-PLAN.md — PDF branding con colores tenant + logo S3 + WCAG
- [x] 03-03-PLAN.md — ConfigPage pestaña Marca: logo upload UI + color pickers
- [x] 03-04-PLAN.md — CatalogoPage + POST /productos/catalogo-pdf + image upload
- [x] 03-05-PLAN.md — Validación E2E + smoke test producción + SUMMARY.md
```
</output>
