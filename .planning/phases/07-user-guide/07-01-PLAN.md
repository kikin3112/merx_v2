---
phase: 07-user-guide
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/servicios/guia_usuario/__init__.py
  - backend/app/servicios/guia_usuario/generator.py
  - backend/app/servicios/guia_usuario/styles.py
  - backend/app/servicios/guia_usuario/templates/chapter.py
  - backend/app/servicios/guia_usuario/templates/section.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "PDF generator can create multi-page document with proper formatting"
    - "Table of contents generates automatically with correct page references"
    - "Brand colors from CLAUDE.md (#EC4899, #8B5CF6) are applied consistently"
    - "Chapter and section templates are reusable for all functionalities"
  artifacts:
    - path: "backend/app/servicios/guia_usuario/generator.py"
      provides: "Main PDF generator class with document assembly"
      min_lines: 100
    - path: "backend/app/servicios/guia_usuario/styles.py"
      provides: "Custom ParagraphStyles matching brand colors"
      min_lines: 80
    - path: "backend/app/servicios/guia_usuario/templates/chapter.py"
      provides: "Reusable chapter template with title, intro, sections"
      min_lines: 50
  key_links:
    - from: "generator.py"
      to: "styles.py"
      via: "import custom styles"
      pattern: "from.*styles import"
    - from: "generator.py"
      to: "templates/chapter.py"
      via: "ChapterBuilder class"
      pattern: "ChapterBuilder"
---

<objective>
Create the PDF user guide infrastructure: generator, styles, and reusable templates using ReportLab Platypus framework.

Purpose: Establish the foundation for building the comprehensive user guide with proper formatting, TOC, and chapter structure following brand guidelines from CLAUDE.md.

Output: Core PDF generation infrastructure ready for chapter content.
</objective>

<execution_context>
@C:/Users/luisr/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/luisr/.config/opencode/get-shit-done/templates/summary.md

# Reference existing PDF service patterns
@backend/app/servicios/servicio_pdf.py
</execution_context>

<context>
@.planning/phases/07-user-guide/07-CONTEXT.md
@.planning/phases/07-user-guide/07-RESEARCH.md

# Brand colors from CLAUDE.md:
# Primary: #EC4899 (Rosa), Secondary: #8B5CF6 (Violeta)
# Success: #10B981, Warning: #F59E0B, Error: #EF4444
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create user guide directory structure and styles</name>
  <files>
    - backend/app/servicios/guia_usuario/__init__.py
    - backend/app/servicios/guia_usuario/styles.py
  </files>
  <action>
    Create directory `backend/app/servicios/guia_usuario/` with:
    1. `__init__.py` - Module exports (GeneratorGuiaUsuario class)
    2. `styles.py` - Define custom ParagraphStyles:
       - Title: 24pt, #EC4899 (brand primary), centered
       - ChapterTitle: 18pt, #1F2937, spaceBefore=30
       - SectionTitle: 14pt, #374151, spaceBefore=20
       - BodyText: 11pt, #333333, leading=16, justified
       - Note: 10pt, #6B7280, italic (for screenshot placeholders)
       - StepList: 11pt with bold "Paso N:" prefix
       Use reportlab.lib.units import cm, mm for consistent spacing.
    Reference existing patterns from servicio_pdf.py but enhance for user guide.
  </action>
  <verify>
    Import styles.py and verify ParagraphStyle objects are created correctly:
    `python -c "from backend.app.servicios.guia_usuario.styles import get_styles; s = get_styles(); print('Title:', s['Title'].fontSize)"` should print 24
  </verify>
  <done>
    Custom ParagraphStyles defined matching brand colors from CLAUDE.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reusable chapter and section templates</name>
  <files>
    - backend/app/servicios/guia_usuario/templates/__init__.py
    - backend/app/servicios/guia_usuario/templates/chapter.py
    - backend/app/servicios/guia_usuario/templates/section.py
  </files>
  <action>
    Create templates module with:
    1. `templates/__init__.py` - Export classes
    2. `templates/chapter.py` - ChapterBuilder class:
       - Constructor: __init__(title, style_dict)
       - add_intro(text): Add chapter introduction paragraph
       - add_section(section_title, content): Add titled section with content
       - add_screenshot_placeholder(description): Add styled box "[IMAGEN: descripción]"
       - add_step_list(steps: list): Add numbered steps (Paso 1:, Paso 2:, etc.)
       - add_feature_table(features: list of dict): Add table with {name, description, available}
       - build(): Return list of Platypus elements
    3. `templates/section.py` - SectionBuilder for nested sections:
       - Similar structure but for sub-sections within chapters
    Use Spacer for consistent spacing between elements (0.3*cm between sections).
  </action>
  <verify>
    Import and instantiate ChapterBuilder, verify it returns list of elements:
    `python -c "from backend.app.servicios.guia_usuario.templates.chapter import ChapterBuilder; from backend.app.servicios.guia_usuario.styles import get_styles; cb = ChapterBuilder('Test', get_styles()); print(type(cb.build()))"`
  </verify>
  <done>
    ChapterBuilder and SectionBuilder classes create proper Platypus element lists
  </done>
</task>

<task type="auto">
  <name>Task 3: Create main PDF generator with TOC and document assembly</name>
  <files>
    - backend/app/servicios/guia_usuario/generator.py
  </files>
  <action>
    Create `generator.py` with GeneratorGuiaUsuario class:
    1. __init__(output_path): Initialize SimpleDocTemplate with A4, margins (2cm)
    2. add_title_page(title, subtitle): Cover page with title, subtitle, version, date
    3. add_toc(): Add TableOfContents with proper levelStyles for Heading1/2
    4. add_chapter(chapter_builder): Add pre-built chapter to document
    5. build(): Generate final PDF
    6. generate() -> bytes: Return PDF as bytes (for S3 upload)
    
    Document structure:
    - Página 1: Título "Guía de Usuario Chandelier ERP/POS"
    - Página 2: Tabla de contenidos
    - Capítulos 1-13: Contenido por funcionalidad
    - Capítulos adicionales: Manual con instrucciones
    
    Include proper page breaks between major sections.
    Enable bookmarks for PDF reader navigation.
  </action>
  <verify>
    Create test PDF and verify it generates without errors:
    `python -c "
from backend.app.servicios.guia_usuario.generator import GeneratorGuiaUsuario
gen = GeneratorGuiaUsuario('test_guia.pdf')
gen.add_title_page('Guía de Usuario', 'Versión 1.0')
gen.add_toc()
gen.build()
import os; print('PDF created:', os.path.exists('test_guia.pdf'))
"`
  </verify>
  <done>
    GeneratorGuiaUsuario creates valid PDF with title page and table of contents
  </done>
</task>

</tasks>

<verification>
- [ ] All imports work without errors
- [ ] Custom styles apply brand colors (#EC4899, #8B5CF6)
- [ ] ChapterBuilder produces proper Platypus elements
- [ ] Table of contents generates with correct structure
- [ ] Test PDF creates successfully with A4 format
</verification>

<success_criteria>
PDF generator infrastructure complete with reusable styles and templates ready for chapter content.
</success_criteria>

<output>
After completion, create `.planning/phases/07-user-guide/07-01-SUMMARY.md`
</output>
